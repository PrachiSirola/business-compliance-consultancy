# Base URL of a separately-hosted backend (leave blank to use the same
# origin / the Vite dev proxy to backend/, which is the default setup).
# Example for a deployed backend: VITE_API_URL=https://api.suits.example.com
VITE_API_URL=
